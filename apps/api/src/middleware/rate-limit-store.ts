/** Shim (PR7 edge re-home): canonical module lives at `src/interface/http/edge/rate-limit-store.ts`. Removal belongs to cutover PR8, not here. */
export * from "../interface/http/edge/rate-limit-store.js";
