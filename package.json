{
  "name": "sistema-beim-system-tech",
  "version": "0.0.0",
  "private": true,
  "description": "Sistema Beim — Monorepo con storefront web, panel de gestión, desktop y móvil",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "generate": "turbo run generate",
    "format": "prettier --write .",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  }
}
