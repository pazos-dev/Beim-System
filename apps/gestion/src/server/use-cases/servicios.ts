// Backward-compatible re-export: canonical home is now src/server/servicios/servicios-use-cases.ts.
// TODO: point new imports at the domain folder; this shim stays until callers migrate.
export * from "../servicios/servicios-use-cases";
